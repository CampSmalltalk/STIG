Defines a file structure representing a package on a disk including the facilities for reading and writing it.

Instance Variables
	root	<String> root of the repository
	directoryCache	<Dictionary> caches visited directories

